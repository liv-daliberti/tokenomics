# LaTeX build artifacts — the sources and style files are what's versioned
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
/acl_latex.pdf
*.synctex.gz
# keep committed figures under fig/ (only the built manuscript pdf is ignored)
