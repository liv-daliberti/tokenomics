# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Experiment outputs (transcripts, runs) — regenerate; do not commit
runs/
report/
*.jsonl
!tests/**/*.jsonl
!docs/samples/**        # committed sample game + rendered report (viewable on GitHub)

# Tooling
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/
.DS_Store

# Model weights / caches — never commit
*.safetensors
*.bin
hf_cache/

# local HF model cache
.hf_cache/
